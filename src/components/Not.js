import React from 'react';
import { Link } from 'react-router-dom';

const Not = () => {
    return (
        <div>
            <Link to="/">
                <button>js</button>
            </Link>
        </div>
    );
};

export default Not;